name: xml-masker-v2
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: XML entity encode via env
        env:
          S: ${{ secrets.XML_TEST_SECRET }}
        run: python3 -c "import os,html; s=os.environ['S']; print(f'XML_ENCODED={html.escape(s)}')"
